# Copy to .env.server; leave the existing Windows .env unchanged.
# Set a new long password, enclosed in single quotes.
POSTGRES_PASSWORD=''
# Local testing only. Later set the Linux server LAN IP.
APP_BIND_IP=127.0.0.1
APP_PORT=8080
